# 04 · 事实校验台账（v2 · 2026-07 · 三轮调研收敛版）

> [!IMPORTANT]
> **归档说明（2026-08-11）**：本文是截至 2026-07-06 的历史收敛台账。下文“全库唯一事实台账”是当时角色，现已由仓库实测证据和 [`docs/facts.md`](../../facts.md) 取代。本文用于来源追溯，不构成当前执行授权；完整权威边界见 [`docs/research/README.md`](../README.md)。

> 日期：2026-07-06。取代 `04_事实校验_硬件模型框架_2026-06.md`。
> 角色：**全库唯一事实台账**——三轮调研（v1/v2/v3，6 份报告）+ 独立核实的全部"已核实/已推翻/待实测"条目，按域组织。写报告、做简历、面试引用一律以本文口径为准。
> 每条带证据等级（官方一手 / 论文 / 社区实测 / 推算 / 待实测）与时效性提示。旧版仍然有效的数据（投机解码负收益锚点、模型规格）已并入，不必回读旧版。

---

## 1. 硬件事实

| 条目 | 内容 | 等级 | 时效 |
|---|---|---|---|
| RK3588 | 16GB，NPU 6 TOPS **3 独立核**；LLM 量化仅 **W8A8**（W4A16 是 RK3576 特性，勿混淆）；所需内存约模型的 1.5–3 倍 | 官方 | 稳定 |
| PC | i5-13500HX + 16GB RAM + RTX4060 8GB（Ada sm_89）；Win11 实际可用显存 ~6.5–7.2GB；**定位=CPU+GPU 混合平台**，非"4060 单卡" | 官方+实测 | 稳定 |
| 12B on PC | Q4 权重 ≈7GB 必溢出、纯 GPU 崩速——仅作混合 offload 实验素材 | 社区实测 | 稳定 |
| 租卡例行层 | AutoDL vGPU-32GB（62–80GB RAM）¥1.58–1.68/时；vGPU-48GB（90–96GB RAM）¥2.88/时；CUDA ≤13.0/13.2；**硅片型号未标注、禁 profiling** | 截图一手 | 价格月级波动 |
| **RTX 6000 96GB** | RTX PRO 6000 Blackwell：**sm_120（CC 12.0）**、188SM/24,064 cores、512-bit GDDR7、官方 ~1.79TB/s、**官方支持 4 实例 Universal MIG**（compute 模式+驱动 580.95+/CUDA 13.0+）；AutoDL ¥5.98/时 | 官方 datasheet | 稳定 |
| **RTX 6000D 84GB** | 中国合规裁剪版：156SM/19,968 cores、448-bit、带宽 **~1.5–1.57TB/s（28Gbps×448bit 推算；早期媒体的 1.1TB/s 已修正）**；MIG **未确认**；¥5.18/时；驱动 595.71 | 推算+一手列表 | MIG 待实测 |
| 选卡裁决 | R-Max 默认 96GB（+12GB 显存/官方确定性/MIG），6000D 兜底；带宽差 ~15% 非早期以为的 45% | 裁决记录 | — |

## 2. RKLLM / 板端生态

| 条目 | 内容 | 等级 | 时效 |
|---|---|---|---|
| RKLLM 版本 | **v1.3.0（2026-06-17）**，CHANGELOG 明文新增 Qwen3.5 / Gemma4 / SmolLM3 支持。**教训存档**：GitHub releases 静态页缓存曾显示 v1.2.3 为 Latest，导致一份调研报告误判——版本一律以 API/CHANGELOG/板上 `rkllm init` 打印为准 | 官方 | 板上复核 |
| 端侧速度锚 | Gemma4-E2B W8A8 官方 bench **11.12 tok/s @2499MB**；Qwen3.5-4B 官方 **6.22 tok/s @4576MB**、社区实测 ~5.7（写"~6 tok/s 级"双源）；Qwen3-1.7B NPU ~13.6 tok/s（GatekeeperZA 权重）；2B 级 ~11.5 tok/s | 官方表+社区 | 版本相关 |
| 预转换权重 | Gemma4 社区**无成熟预转换权重**（与官方 bench 表存在不矛盾）→ 自转换并发布 = 公开杠杆 | 检索确认 | 月级复核 |
| RKLLama multiload | "multiload models in memory"=**不同模型 stream 并行**（非 stream FIFO、内存不足卸载最旧），**不是同模型多实例**；自动 prompt cache 默认保留 7 天 | 一手 README | 稳定 |
| num_npu_core 分核双实例 | 转换设 1/2 核后双实例同时推理是否被驱动允许——**无一手实测，全库最高优先待实测项** | 待实测 | 板上十分钟出结论 |
| prompt cache API | `RKLLMPromptCacheParam{save_prompt_cache, prompt_cache_path}`；`rkllm_load_prompt_cache/release`；`rkllm_clear_kv_cache(keep_system_prompt, [start,end))` 仅 keep_history==0 且暂停时；多 cache 文件应用层自管 | 一手头文件 | 板上 grep 复核 |
| 转换坑 | 校准集官方示例仅 21 条通用样本→换 agent 域含工具 schema 样本；chat template 与训练一致（v1.2.3+ 嵌 thinking 模板）；量化塌陷先例 DeepSeek-R1-Distill 输出 [PAD]（Issue #424）→ 转换后先格式 smoke | 官方+issue | 稳定 |
| MLC-LLM on Mali | 官方文档支持 OPi5/RK3588 OpenCL 路径，但性能数据停在 2024（Llama3-8B ~2.3 tok/s），2026 对 Qwen3/Gemma4 支持**无证据** → P2 自测后再排期 | 官方旧+检索空 | 自测定 |
| rk-llama.cpp | GGUF-on-NPU 实验性 fork，只观察不押注 | 社区 | — |

## 3. 模型与训练事实

| 条目 | 内容 | 等级 |
|---|---|---|
| Qwen3.5 家族 | 0.8B/2B/4B/9B/27B/35B-A3B/122B-A10B；**全家族禁 QLoRA（Unsloth 官方明文，"no matter MoE or dense"）→ 一律 bf16 LoRA**；Qwen3 系不受此限 | 官方 |
| Unsloth 显存锚 | Qwen3.5 bf16 LoRA：0.8B≈3G / 2B≈5G / 4B≈10G / 9B≈22G / 27B≈56G；35B-A3B≈74G（H100-80GB seq4096 已有 OOM 先例→96GB 卡短序列）；Qwen3-30B-A3B 16bit LoRA≈63G | 官方口径 |
| Gemma4 训练坑 | ① E2B/E4B 跨层 KV 共享（num_kv_shared_layers=20/18）× `use_cache=False` → loss 发散（20-step smoke 强制）；② fp16 attention mask -1e9 溢出 → 一律 bf16；③ 官方建议"训 E4B QLoRA 优于 E2B LoRA"（E2B LoRA 8–10G / E4B LoRA 17G / 31B QLoRA 22G / 26B-A4B LoRA >40G）；④ GKD 场景 `attn_implementation="kernels-community/flash-attn2"` | 官方/一手 issue |
| Gemma4 家族规格 | E2B(2.3B eff)/E4B(4.5B eff)/12B/26B-A4B(3.8B act)/31B；Q4 显存 E2B≈2G/E4B≈3.5G/12B≈8G/26B-A4B≈14.4G/31B≈17.5G；MTP head：E2B 78M/E4B 156M/31B 930M | 官方+社区口径 |
| think 块处理 | Fable 训练模板保留 `<think>`；Gemma4 官方"保留 ≥75% reasoning" → 能力轨保留、端侧变体第二渲染 | 一手 |
| 蒸馏 | 在线同卡蒸馏无先例 → 两段式离线为主；GKDTrainer P2 流程验证 | 检索确认 |
| CUDA 13.2 坑 | Unsloth 官方明禁恰好 13.2（Qwen3.6 乱码）→ 镜像 13.0/13.1 | 官方 |

## 4. 推理引擎与 sm_120 生态坑（R-Max 开机必读）

| 条目 | 内容 | 等级 |
|---|---|---|
| llama.cpp 版本锚 | 本地锁 **b9553**（Qwen3.6 MTP known-good；b9702+ 回归）；`--parallel`+cont-batching、`-ngl`、`--n-cpu-moe`、`--cache-type-k/v`、混合模式 `-ub 2048`、`--no-mmap` | 一手 issue/PR |
| MTP 现实 | llama.cpp 已合入（Unsloth 确认）；参考锚 RTX 6000 上 27B MTP 160 tok/s、35B-A3B 240 tok/s；**消费卡单流可能净负收益**（旧版锚点维持：RTX3090 级 Q4 batch=1 各投机模式普遍负收益，最差 −44.6%；vLLM MTP k=1 关 prefix cache 时 +27.5%——同硬件不同引擎符号相反）；Gemma4 MTP：dense >2×，**E2B/E4B 变体不工作**（issue #22337 未解）；MTP on Marlin 路径 −22% | 社区实测（面试引用注明） |
| DSpark 状态 | vLLM **main 已合入**（PR #46995 核心 fixed-γ + #47093 speculators，合入点 commit **2b753ad** 2026-07-02）；仅 nightly、无 release tag；动态置信调度 #47513 open——**不得声称论文 Algorithm 1 完整实现**；Gemma4-12B draft #47216 open；官方验证仅 8×B300(sm_100)；sm_120 社区先例 1.08–1.24× vs MTP（自定义镜像）；τ 用主线计数器 `spec_decode_num_accepted_tokens_per_pos`；红线：**DeepSeek-V4 单卡不可行（~180GB）** | PR 链一手 |
| DSpark 本地 | llama.cpp `draft-dspark`（PR #25173）未进主线 → P2 fork 验证；ankk98 GGUF 需 ft-dspark 分支语义 | 一手 |
| sm_120 三坑 | ① **NVFP4 KV crash**：`--kv-cache-dtype nvfp4` 首请求崩（issue #43562，驱动 595.71 同款）；根源 QMUL4 反量化指令在消费 Blackwell 不存在（fp4→fp16→bf16 转换链 ~9 指令/MMA，fp4 prefill 反而慢 1.8×——A4Q 实测）；TRT-LLM 13,453 个预编译 attention kernel 无 sm120 目录；② **Marlin 回退**：NVFP4 MoE 的 CUTLASS 原生路径在 SM120 有 broken→Marlin W4A16 先例（日志查关键字）；③ **flash-attn 缺位**：pip flash-attn 官方不支持 sm_120（issue #1987）→ 禁装，用 vLLM 内建 | 一手 issue+A4Q |
| sm_120 正路 | NVFP4 **权重**（GEMM）路径成熟（vLLM b12x FP4 GEMM 主线）；**KV 默认 FP8**（官方博客：Blackwell FP8 KV+attention 恢复 93–96%）；PyTorch 需 nightly cu128/129（stable ≤2.4.1 无 sm_120） | 官方 |
| NVFP4 精度 | 大模型(8B+) 恢复 >99%（RedHat/NVIDIA）；**Qwen3-4B PTQ：AIME 79.6→62.9(−17pp)/GPQA −4.4/LCB −6.8，退化集中中高难度，QAD 可恢复**（arXiv 2606.05682）→ 评测必须难度分层；现成 checkpoint：RedHatAI/Qwen3-14B-NVFP4、nvidia/Qwen3.6-27B-NVFP4(19.7GB)、nvidia/Gemma-4-31B/26B-A4B-NVFP4 等 | 论文+官方 |
| prefix cache | vLLM V1 默认开、近零开销；llama.cpp server `--slot-save-path`+稳定 slot ID（历史 b8825–b8891 bug 段教训保留）；**compaction/投机会冲击命中率**（vLLM #38182：MTP 使命中 ~92%→~71%） | 官方+issue |
| 量化质量锚 | GGUF Q4 code ~92% FP16 / Q5 ~95% / Q8 ~98%；Q4_K_M 社区收敛点 | 社区口径 |

## 5. Benchmark 时效小节（引用口径，防旧结论回流）

| 条目 | 内容 | 等级 |
|---|---|---|
| **SWE-bench Verified 已弃用** | OpenAI 2026-02-23 官宣（污染 + 59.4% 失败例测试本身缺陷）——文档/报告/面试统一口径；替代：**SWE-bench Pro 公开子集**（ScaleAI/SWE-bench_Pro + `jefzda/sweap-images:{tag}` 镜像，`--instance_ids` 10–20 题，≥120GB 盘/16GB RAM）或 SWE-rebench（~7,500 清洗镜像） | 官方一手（本库独立核实） |
| Terminal-Bench | 主 benchmark 锚 **2.1**（89 任务，修复 28/89，修复后无未解任务）；网页任务浏览器一度滞后≠数据不可拉——开工日 `harbor run -d terminal-bench/terminal-bench-2-1 -a oracle -l 5` 探测定案；接入 terminus-2 + EvalScope `eval_type='openai_api'`；参考锚：前沿 <65%、小模型 ~15%、Fable 5 模型分 84.3%；小模型 2.0→2.1 分差 ~3pp | 官方+论文 |
| 纠偏备忘 | ByteDance-Seed/**EdgeBench ≠ 端侧**（长时程环境学习，每任务 ≥12h）；Qwen/**AgentWorldBench ≠ 成功率 benchmark**（世界模型评测；其 9 源任务 query 可复用） | 检索确认 |
| 通用锚 | HLE/GPQA 小子集仅作 Source-C"不塌陷"回归（Mercor 先例：HLE 掉 1.3% 不显著） | 先例 |

## 6. 数据集事实

| 条目 | 内容 | 等级 |
|---|---|---|
| Glint/Fable-5-traces | 4,665 Pi sessions（60 source sessions），AGPL-3.0；与 armand0e/claude-fable-5 同源二选一 | 一手卡片 |
| Crownelius 2M | 2.0M 行 981.5MB，标注 MIT 但内容源 AGPL → 能力轨抽样+row_hash 去重 | 一手 |
| **Fable 语料冻结** | claude-fable-5 2026-06-10 短暂开放、**06-22 因出口管制全球下线**——语料不可再生，**立即存档+checksum** | 一手 |
| Open-SWE-Traces | **207,489** 轨迹/9 语言/20k 真实 PR/CC-BY-4.0/18.3GB；config openhands 105k+sweagent 102k；**Table 5：含 unresolved 全量优于 resolved-only（推翻本库旧裁决）**；**跨 harness 惩罚**成立 → Pi 格式对齐必要 | 论文一手 |
| 其余 | Hermes filtered 7,646→3,679(Apache)；GLM-5.2-Agent 319 JSONL；qwen3.7-max-pi-traces（Stage-1）；Mythos-25k(Apache, 无 tools schema, 防遗忘锚)；**全部公开集无 subagent 并发标注** → 自采是唯一来源 | 一手 |
| 合规边界 | AGPL §13 触发点=网络服务/分发；本地训练+不发权重+发报告=低风险；能力轨权重永不发布（RELEASE_POLICY） | 条款事实 |

## 7. 已推翻结论清单（引用任何旧文档前先查这里）

完整 14 条见《文档更新计划 v2》§2。高频误引 Top5：① "OpenCode>Pi 排名/OpenCode 底座"（旧 04 TL;DR）→ Pi-first；② "SWE-bench Lite/Verified 子集" → Pro；③ "NVFP4 需 Blackwell、可租卡均不支持、归入只读"（旧 12 FAQ Q4）→ **R-Max 可租 Blackwell，NVFP4 权重路径已是 P1**；④ "48G 只做对照不做主场" → 三层训练矩阵+R-Max；⑤ "端侧 agent 不作第三支柱"（旧 00 决策⑨）→ v3 三支柱（记录于 00 增补）。

## 8. 复核纪律

- 面试前一周：复核 star 数/版本号/价格类快变条目（本表"时效"列）。
- 上板/开机实测推翻本表任何条目 → 当日更新本表并在条目旁标 `[实测修正 YYYY-MM-DD]`。
- 本表只进事实，不进裁决；裁决看 07_v3/08_v3 与裁决链。
