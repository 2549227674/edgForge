# M0 evaluation base

- 2026-08-02 — TB 2.1 probe `terminal-bench/kv-store-grpc`: endpoint request/reply, container command execution, and verifier result all observed (`probe_link_closed=true`); reward `0.0` is a link probe only and does not enter task selection or baseline scoring. Evidence: `results/m0_tb_probe/2026-08-02__16-31-43/`.
