# M0 配置索引

- `m0_baseline_job.yaml`：B0 Terminal-Bench 2.1 锁定 20 题×5 的 Harbor job 定义，是冻结合同的一部分。
- `m0_allowlist_check.yaml`：WSL 阶段的运行期外网检查配置，仅作历史复现。
- `m0_allowlist_check_vm.yaml`：原生 Ubuntu VM 的 allowlist 检查配置，仅作历史复现；一次性 VM 已删除。

M0 已冻结，这些文件不构成新的运行授权。
